name: Auto Update _worker.js

on:
  schedule:
    - cron: '0 0 */3 * *' # 每隔3天执行一次
  workflow_dispatch:       # 允许手动触发
    inputs:
      release_type:
        description: '选择更新版本类型' # 选择更新版本类型
        required: false
        default: 'release'

jobs:
  update-worker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-submodules: true

      # 创建或更新 _worker.js
      - name: Create or update _worker.js
        run: |
          if ! git check-ignore -q _worker.js; then
              echo "Creating new _worker.js"
              cat << EOF > .github/workflows/_worker.js
# This is an auto-generated file by GitHub Actions Workflow
# Do not modify this file manually!

name: Auto Update _worker.js

on:
  schedule:
    - cron: '0 0 */3 * *' # 每隔3天执行一次

jobs:
  update-worker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-submodules: true

      # 其他步骤如同之前的工作流程...
            EOF
            git add .github/workflows/_worker.js
            git commit -m "Update _worker.js on $(date)"
            git push origin main
          else
              echo "Updating _worker.js"
              cat << EOF > .github/workflows/_worker.js
# This is an auto-generated file by GitHub Actions Workflow
# Do not modify this file manually!

name: Auto Update _worker.js

on:
  schedule:
    - cron: '0 0 */3 * *' # 每隔3天执行一次

jobs:
  update-worker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-submodules: true

      # 其他步骤如同之前的工作流程...
            EOF
            git add .github/workflows/_worker.js
            git commit -m "Update _worker.js on $(date)"
            git push origin main
          fi

      # 创建或更新 version.txt
      - name: Create or update version.txt
        run: echo "$(git rev-parse HEAD)" > version.txt

      # 发送通知
      - name: Send update success notification (reuse or create GitHub Issue)
        uses: actions/github-script@v6
